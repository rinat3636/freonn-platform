/**
 * Точка входа приложения — `server/_core/index.ts` (см. package.json: dev, build, start).
 * Этот файл не запускает HTTP-сервер и оставлен, чтобы не путать со старым шаблоном «только static».
 */
export {};
